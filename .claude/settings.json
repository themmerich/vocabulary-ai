{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "cd frontend && if [ -d node_modules ]; then npm run format && npm run lint && npm run build; else echo 'Stop hook skipped: frontend/node_modules missing (run npm install)'; fi",
            "timeout": 300,
            "statusMessage": "frontend: format → lint → build"
          }
        ]
      }
    ]
  }
}
