// Template for `environment.local.ts`.
//
// Copy this file to `environment.local.ts` (which is gitignored) and paste your
// PrimeUI Community license token below. During `npm start` (development build)
// Angular replaces `environment.ts` with `environment.local.ts`, so the token is
// only ever read from your local copy and never committed.
//
//   cp src/environments/environment.local.example.ts src/environments/environment.local.ts
export const environment = {
  primeNgLicense: 'PASTE-YOUR-PRIMEUI-COMMUNITY-TOKEN-HERE',
};
