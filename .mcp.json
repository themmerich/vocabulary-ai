{
  "mcpServers": {
    "angular-cli": {
      "type": "stdio",
      "command": "cmd",
      "args": [
        "/c",
        "cd /d %CLAUDE_PROJECT_DIR%\\frontend && npx -y @angular/cli mcp"
      ]
    }
  }
}